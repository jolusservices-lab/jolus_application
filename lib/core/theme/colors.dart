import 'package:flutter/material.dart';

class JolusColors {
  // Brand Colors - V7 Identity
  static const primaryBlue = Color(0xFF1F6FE5); // Azul Principal V7
  static const darkBlue = Color(0xFF00236F);    // Azul Oscuro (Títulos y énfasis)
  static const accentBlue = Color(0xFF2FA4FF);
  static const lightBlue = Color(0xFFF0F6FF);

  // UI Palette
  static const background = Color(0xFFF8F9FE);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceLow = Color(0xFFF4F3FA);
  
  // Status Colors
  static const success = Color(0xFF27AE60);
  static const error = Color(0xFFEB5757);
  static const warning = Color(0xFFF2994A);
  static const info = Color(0xFF1F6FE5);

  // Compatibility / Legacy Colors (Redirected to V7)
  static const primary = darkBlue;
  static const primaryContainer = Color(0xFF1E3A8A);
  static const tertiary = Color(0xFF4B1C00);
  static const onBackground = Color(0xFF1A1C1E);
  static const outlineVariant = Color(0xFFC5C5D3);
  static const onSurfaceVariant = Color(0xFF444651);
}
